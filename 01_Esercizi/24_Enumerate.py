names = ['Apple', 'Microsoft', 'Samsung']

for i, name in enumerate(names): # enumerate(names) restituisce una coppia (i, name) per ogni elemento della lista names, dove i è l'indice dell'elemento e name è l'elemento stesso
    print(f"{i+1}: {name}")